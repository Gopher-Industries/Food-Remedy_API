# Personalization PR dependency review — 2026-09-26

The backend feature runs from BE058 through BE070 in thirteen one-ticket PRs, [#262](https://github.com/Gopher-Industries/Food-Remedy_API/pull/262) through [#274](https://github.com/Gopher-Industries/Food-Remedy_API/pull/274). Each targets the preceding ticket branch, with BE058 targeting BE039 [#222](https://github.com/Gopher-Industries/Food-Remedy_API/pull/222). BE039 targets BE037 [#221](https://github.com/Gopher-Industries/Food-Remedy_API/pull/221), then BE036 [#220](https://github.com/Gopher-Industries/Food-Remedy_API/pull/220), then BE034 [#207](https://github.com/Gopher-Industries/Food-Remedy_API/pull/207). Merge in that order after resolving the base chain; none of the personalization PRs is a standalone merge into current `main`.

## Findings

1. **Base conflict:** GitHub reports #207 as `DIRTY` against `main`. A read-only `git merge-tree --write-tree origin/main BE070-jev-observability-rollout` also reports conflicts in `FIRESTORE_STRUCTURE.md`, product detail schema, auth/profile sync tests, `sqlConfig.ts`, package files, allergen/recommendation services, and product normalization/detail services. This result is an integration warning, not an attempted merge. Resolve #207 and then recheck each dependent PR before merging.
2. **SQLite migration collision:** Current `main` already has the account-scoped product-history migration at `user_version = 6`. The BE059/BE060 stack was authored on a branch ending at v5 and adds v7/v8. The `sqlConfig.ts` conflict must retain the v6 migration and then run v7/v8. Run the migration and history tests on the resolved merge result before merge; choosing either side wholesale would risk data loss or skipped migrations. The open BE027 [#214](https://github.com/Gopher-Industries/Food-Remedy_API/pull/214) also touches `sqlConfig.ts` and is currently `DIRTY`.
3. **Deletion flow boundary:** BE047 [#243](https://github.com/Gopher-Industries/Food-Remedy_API/pull/243) changes the app from immediate client cleanup to an authenticated account-deletion **request**. Its PR contains request acceptance and persistence, but no worker that completes account deletion. BE059/BE060 cleanup currently runs in the existing client deletion path. Before shipping the new request flow, a server deletion worker must remove preferences, saved intents, sessions, events, profile records and other account data and expose completion evidence. The BE047 PR should not be read as that worker.
4. **Retrieval paths:** BE035 [#236](https://github.com/Gopher-Industries/Food-Remedy_API/pull/236) improves the client Firestore category candidate pool. BE064 [#268](https://github.com/Gopher-Industries/Food-Remedy_API/pull/268) uses a separate bounded server hybrid retrieval path for authenticated v2 substitutions. There is no direct file conflict in the reviewed diffs, but mobile integration should consume v2 rather than mix the two candidate lists.
5. **Deployment configuration:** BE058 production configuration [#245](https://github.com/Gopher-Industries/Food-Remedy_API/pull/245) is a different ticket with the same number as personalization BE058 #262. Its validator checks the existing public API base URL and build requirements; it does not establish a server-capable host or validate the new personalization API base URL. The current Expo static web export skips API routes. Confirm hosting and endpoint configuration before enabling the v2 mobile client or Jev.
6. **Cross-cutting changes:** BE057 [#244](https://github.com/Gopher-Industries/Food-Remedy_API/pull/244) changes profile-sync error handling and dependencies. Re-run privacy and sync tests after reconciling it with BE059–BE060. BE059 history secret scan [#250](https://github.com/Gopher-Industries/Food-Remedy_API/pull/250) is another unrelated ticket-number collision.

The BE069 and BE070 network-free evaluation checks passed on GitHub when reviewed. Earlier stacked PRs had no check run in their status at this snapshot; the new workflow starts in BE069. Local full mobile Jest, Firestore emulator rules and the backend privacy/rollout tests passed on the final stack. Full TypeScript checking still reports pre-existing errors in `membersEdit.tsx`, `theme.ts` and absent product JSON modules. No PR was merged, no production configuration was changed and no review comments were posted to other contributors' PRs.
